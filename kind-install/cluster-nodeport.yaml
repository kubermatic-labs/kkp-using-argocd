# Cluster that create one master and one node and is ready for nginx-ingress
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
# Redirect pulls for these upstream registries through local pull-through-cache
# mirrors (see kind-install/registry-mirror.sh) so repeated `kind delete/create`
# cycles don't re-download the same images from the internet every time. The
# mirror containers are connected to the "kind" podman network by
# connectRegistryMirrorsToKindNetwork() in run-local-kind.sh, so they're
# reachable from cluster nodes by container name.
#
# This is a static duplicate of registry-mirror.sh's REGISTRY_MIRRORS (kind's
# own config is a plain YAML file, not rendered at runtime) -- keep both in
# sync if the mirror list changes. join-usercluster-node.sh's worker
# container needs the identical config applied separately (it never goes
# through `kind create cluster`, so doesn't get this file at all), via
# registryMirrorContainerdConfig() in registry-mirror.sh.
containerdConfigPatches:
- |-
  [plugins."io.containerd.grpc.v1.cri".registry.mirrors."docker.io"]
    endpoint = ["http://kind-registry-docker:5000"]
  [plugins."io.containerd.grpc.v1.cri".registry.mirrors."quay.io"]
    endpoint = ["http://kind-registry-quay:5000"]
  [plugins."io.containerd.grpc.v1.cri".registry.mirrors."registry.k8s.io"]
    endpoint = ["http://kind-registry-registryk8s:5000"]
  [plugins."io.containerd.grpc.v1.cri".registry.mirrors."ghcr.io"]
    endpoint = ["http://kind-registry-ghcr:5000"]
nodes:
- role: control-plane
  kubeadmConfigPatches:
  - |
    kind: InitConfiguration
    nodeRegistration:
      kubeletExtraArgs:
        node-labels: "ingress-ready=true"
  extraPortMappings:
  # Host ports are >=1024 because rootless podman can't bind privileged ports
  # without a host-wide sysctl change. Container-side ports stay 80/443 since
  # that's what the ingress-nginx pod itself listens on.
  - containerPort: 80
    hostPort: 8080
    protocol: TCP
  - containerPort: 443
    hostPort: 8443
    protocol: TCP
  extraMounts:
  # Persists ArgoCD repo-server's git clone cache (see run-local-kind.sh's
  # ARGOCD_REPO_CACHE_HOST_DIR / installArgoCD's repoServer.existingVolumes.tmp
  # override) across `kind delete/create` cycles -- same idea as the registry
  # mirrors above, but for git clones of the (large) kkpRepoURL repo instead
  # of container images. Directory must exist on the host and be owned by
  # uid 999 before this runs; run-local-kind.sh's ensureArgoRepoCacheDir()
  # does that.
  - hostPath: /var/lib/local-kind-argocd-repo-cache
    containerPath: /mnt/argocd-repo-cache
  # Enable this, if you want explicit workers
  # - role: worker
  # - role: worker